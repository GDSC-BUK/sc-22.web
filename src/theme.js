import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    dashboardColor: {
      blue: "#497eb2",
      white: "#eaac00",
      yellow: "#f8e6c0",
      green: "#4e8d53",
      pink: "#ecaca2",
    },
  },
});

export default theme;
